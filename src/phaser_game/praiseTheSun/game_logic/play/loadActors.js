import Phaser from 'phaser';

import GAME from '../../gameVariables'
import Automata from '../automata'
const loadActors = (playState) =>{
	let self = playState;
	// console.log("loadActors.js -> self", self)


}

export default loadActors;